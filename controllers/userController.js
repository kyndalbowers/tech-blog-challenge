const userController = {
    login: async (req, res) => {
      // Validate user credentials, create a session, and log in the user
      // Use bcrypt to compare the password with the hashed password in the database
    },
    // Other user controller functions (registration, profile, logout)
    };